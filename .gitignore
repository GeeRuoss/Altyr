# macOS
.DS_Store
.AppleDouble
.LSOverride

# Secrets / config locale (ne JAMAIS committer)
.env
.env.*
.secrets/
*.key
*.pem
config.local.*

# Sources HD / archives de travail (hors repo)
all/
sources/
*.psd
*.ai
*.sketch

# Matériel de référence / source (ne pas déployer)
assets/inspi et images/
assets/SVG/
assets/font/

# Éditeurs
.vscode/
.idea/
*.swp

# Logs / temp
*.log
tmp/
